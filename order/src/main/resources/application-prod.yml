spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: jdbc:mysql://{rds endpoint}:3306/
    username:
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
  data:
    redis:
      host:
      port:
      username:
      password:
eureka:
  client:
    service-url:
      defaultZone:
management:
  zipkin:
    tracing:
      endpoint: